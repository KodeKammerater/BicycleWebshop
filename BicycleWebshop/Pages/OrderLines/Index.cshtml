@page
@model BicycleWebshop.Pages.OrderLines.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderLine[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderLine[0].Bicycle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderLine[0].Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderLine) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order.OrderID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bicycle.BicycleID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OrderLineID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OrderLineID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OrderLineID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
